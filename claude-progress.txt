Date: 2026-06-22
TaskStatus: completed
Task scope: M1 回环 (F1-F7) + M3 控制面/设备发现 (F8-F9) + F10 WebSocket 信令 + Mac daemon。
真跨公网 STUN/TURN/coturn/K8s 属 M4（需真网络，本机不可测）。

Current summary:
- M1 (F1-F7) completed：aiortc 回环链路；obs 按采集时间戳重组、send_action、看门狗。
- M3 控制面 (F8-F9) completed：control DataChannel + RPC；设备发现 list_ports/list_cameras/
  find_port(begin→result 事件驱动)；物理 ID 只在 Mac，云端只有逻辑名。
  - 新增 LocalDeviceInventory（包 lerobot find_available_ports / find_*_cameras），
    mac_daemon `--real-devices` 开关枚举真实串口/相机；真串口经控制面回环已验证。
  - 真相机推流：CaptureAgent 接 camera(read_latest) + _fit_frame 归一到声明尺寸；
    mac_daemon `--real-camera <index|/dev/videoN>` 开 OpenCVCamera 推流（daemon 持有相机生命周期，跨 session 复用）。
    回环用 FakeCamera 验证帧颜色穿过 VP8 编解码到云端（18 passed）。关节/send_action 仍是合成，属 M2。
- F10 completed：真 Mac daemon ↔ 云端 controller。
  - signaling.py 加 WebSocketSignaling client；signaling_server.py 信令 relay（按 session/role 转发 SDP，
    缓冲晚到方，对端断开发 bye）。
  - mac_daemon.py：常驻入口，连接→offer→服务一次 session→断连停力(force_safe_stop)→重连循环；
    可 `python -m ...mac_daemon --signaling-url ws://...` 跑。
  - WebRTCProxyRobot 支持 signaling_url=ws://（controller 模式，不起本地 agent）+ ice_servers 配置。
  - CaptureAgent 加 connectionstatechange→closed 事件 + wait_closed()；ICE 可配。
- 测试：tests/robots/test_webrtc_proxy_{alignment,loopback,control,daemon}.py 共 14 passed
  （需 `-p no:hydra_pytest` 绕开本环境无关损坏插件）。daemon 测试用同机三 loop（relay+daemon+cloud）
  走真 localhost WS 信令 + WebRTC host candidate，含「daemon 活过一次 session 再服务下一次」断连重连用例。
- demo_loopback 保留同进程回环演示（控制面 + 看门狗）。

架构要点（回答用户两问）:
- WebRTCProxyRobot 在云端跑；Mac 必须跑常驻 daemon（mac_daemon.py，分支名 webrtc-daemon）。
- 设备 port/camera ID 只在 Mac 端，云端经控制面 RPC 发现，不进云端 config。

M2 完成（接真实 so_follower）:
- CaptureAgent 接 robot=：关节+相机来自一次 robot.get_observation()；_apply_action→robot.send_action；
  看门狗 _safe_stop→robot.bus.disable_torque() 切扭矩(P0)；session 起/动作恢复 enable_torque。
  所有串口 IO 走单线程 ThreadPoolExecutor（不阻塞公网 loop + 总线不并发）。run_daemon 加 robot=。
- examples/webrtc_remote_so100：mac_daemon_so100.py(真 SO100+run_daemon) + cloud_teleop_so100.py
  (WebRTCProxyRobot + 复用 web_so100 jog 面板远程遥操) + README(onboarding+三件套)。
  关键展示：WebRTCProxyRobot 是 drop-in Robot，现有 teleop/record 代码无改即可远程驱动真臂。
- FakeRobot 测试通过（共 24 测试）；真硬件验证靠 example（需真臂）。

Main gaps (M4，需真网络/硬件):
- STUN/TURN(coturn) 真 url + NAT 穿透；K8s hostNetwork/announced IP；relay 多租户+鉴权。
- 多相机（每路一 media track）；send_action 真 clip/ack（目前 robot.send_action 已返回实际下发值）。
- M5: paradigm 落地决策（影响 action DataChannel 载荷）。

架构清理（应 review 意见）:
- WebRTCProxyRobot 改为**纯云端 controller**：删掉 in-process loopback 分支（不再在云端类里起 Mac agent），
  connect() 必须给 ws:// signaling_url。自包含测试/demo 改用「同进程 relay+daemon+controller 三 loop」
  （tests/robots/conftest.py 的 webrtc_link fixture；demo_loopback.py 重写）。
- send_action 早已实现（send_action→_ch_action→_on_action→_apply_action）；只有 _apply_action 真正驱动电机是 M2 stub。
- 相机采集配置归属 Mac（index 永不下发）；云端只声明 obs 形状，get_observation 兜底 _fit_frame；
  set_camera_plan 把期望尺寸推给 Mac（省带宽）。推流相机 open 改 native+resize 回退。
- grab_camera 控制面 RPC 保留（onboarding 单帧预览，走 DataChannel，与连续 obs 的 media track 分离）。

关键踩坑（已解决）:
- aiortc 对象须在 bg event-loop 线程内构造；iceServers=[] 用 host candidate（受限网络/同机），
  默认 None 会走 Google STUN 卡死。

Next steps:
- 等产品方 paradigm 决策；按优先级 M2(真硬件) > M4(coturn/K8s)。
