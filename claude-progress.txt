Date: 2026-06-22
TaskStatus: completed
Task scope: M1 回环链路 (F1-F7) + M3 控制面/设备发现 (F8-F9)。F10 信令/STUN/TURN 基础设施未做（需真网络）。

Current summary:
- M1 (F1-F7) completed：aiortc 回环链路打通，obs 按采集时间戳重组、send_action、看门狗。
- M3 控制面 (F8-F9) completed：
  - 新增 control DataChannel (ordered+reliable) + RPC (RpcRequest/RpcResponse)。
  - control.py: DeviceInventory 协议 / SyntheticInventory / ControlServer(Mac) / ControlClient(cloud)。
  - 设备发现 RPC：list_ports / list_cameras / find_port_begin+find_port_result（事件驱动两段式，
    把 lerobot-find-port 的阻塞 input() 拆成 begin→人拔线→result）。
  - 云端 WebRTCProxyRobot 暴露 list_ports()/list_cameras()/find_port_begin()/find_port_result()。
- 决策：设备发现走云端控制面（标准做法），不并入 resolve_cameras.py 的 Mac 本地指纹方案（个人脚本，已否决）。
- 测试：tests/robots/test_webrtc_proxy_{alignment,loopback,control}.py 共 12 passed
  （需 `-p no:hydra_pytest` 绕开本环境无关的损坏 pytest 插件）。
- demo_loopback 增加控制面发现演示段，已验证。

Main gaps (后续里程碑):
- F10 / M3-infra: WebSocket 信令(aiohttp, localhost 可测) + STUN/TURN(coturn) 配置入口；本机网络受限未做。
- M2: 真 so_follower + 相机；真 LocalDeviceInventory(包 lerobot-find-port/find-cameras)；多相机；send_action 真 clip/ack。
- M4: 自管 K8s (hostNetwork/announced IP/coturn)。
- M5: paradigm 落地决策（影响 action DataChannel 载荷）。

关键踩坑（已解决，见上一阶段）:
- aiortc 对象须在 bg event-loop 线程内构造；iceServers=[] 禁 STUN（受限网络下默认 Google STUN 卡死）。

Next steps:
- 等产品方 paradigm 决策；其余按优先级 M2(真硬件) > M3-infra(信令) > M4。
